import { TimeframeFilterPipe } from './timeframe-filter.pipe';

describe('TimeframeFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TimeframeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
